"""Output writing helpers."""

from pathlib import Path

import pandas as pd


def write_scored_output(df: pd.DataFrame, output_path: Path) -> None:
    output_path.parent.mkdir(parents=True, exist_ok=True)
    df.to_csv(output_path, index=False)

